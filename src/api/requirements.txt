azure-functions>=1.18.0
azure-servicebus>=7.12.0
azure-identity>=1.17.0
# PyJWT, cryptography, and azure-cosmos were only needed by auth.py /
# scan_job.py -- moved out of this package to future-work/auth/ (see its
# README.md). Not needed by the active Gateway right now.
