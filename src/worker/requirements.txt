azure-functions>=1.18.0
aiohttp>=3.10.0
beautifulsoup4>=4.12.0
cryptography>=42.0.0
certifi>=2024.0.0
packaging>=24.0
lxml>=5.0.0
azure-cosmos>=4.5.0
# Managed Identity auth for the Cosmos DB write path in main_scanner.py
# (build_cosmos_container) -- same version floor as src/api/requirements.txt.
azure-identity>=1.17.0
dnspython>=2.6.0
tldextract>=5.3.2
# osv_cve_sync.py's Timer Function: writes the OSV CVE cache to Table
# Storage (Managed Identity, same DefaultAzureCredential as Cosmos DB
# above) and fetches from OSV.dev via a plain synchronous POST -- the
# Timer Function itself is sync, not async like the scan pipeline, so
# this uses `requests` rather than aiohttp.
azure-data-tables>=12.5.0
requests>=2.31.0
