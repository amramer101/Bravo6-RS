# Prototype scouts

`test_03_mixed_content.py` and `test_16_ai_exposure.py` are outside the active Worker's plugin directory. They are not included in the ten-scout evaluation. `superseded/` retains older implementations for context.

Do not move these files into the Worker as a cosmetic cleanup: filename-based discovery could change the runtime. No claim of validated accuracy or deployment is made for these prototypes.

[Active scout reference](../../docs/software-architecture.md)
