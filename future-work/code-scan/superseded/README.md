# Superseded scout implementations

This directory retains older SRI and dependency-scout files outside the active plugin path. The active implementations are `src/worker/test_09_sri.py` and `src/worker/test_10_hallucinated_deps.py`.

Historical source is not evidence that either implementation was executed in a particular cloud build. Preserve filenames and contents for provenance; consult the [current interpretation](../../../docs/software-architecture.md) before reusing older descriptions.
