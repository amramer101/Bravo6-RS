# Superseded prototypes

These files were early, differently-numbered drafts of checks that have since shipped in the
live scanner under different names/numbers. They are kept only for historical reference —
**do not cite these filenames or their `test_NN` numbers**; use the shipped versions.

| Draft here | Shipped as |
|---|---|
| `test_14_subresource_integrity_sri.py` | `src/worker/test_09_sri.py` |
| `test_15_hallucinated_deps.py` | `src/worker/test_10_hallucinated_deps.py` |

Still-unshipped prototypes remain one level up in `future-work/code-scan/`
(`test_03_mixed_content.py`, `test_16_ai_exposure.py`).
